name: Prueba Node.js

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Usar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Ejecutar prueba
        run: node test.js